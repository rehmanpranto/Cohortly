# Database
DATABASE_URL="postgresql://username:password@your-neon-host.neon.tech/your-database?sslmode=require"

# JWT Secrets (CHANGE THESE IN PRODUCTION)
JWT_ACCESS_SECRET="your-super-secret-access-token-change-this-to-random-string"
JWT_REFRESH_SECRET="your-super-secret-refresh-token-change-this-to-random-string"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"

# Server
PORT=5000
NODE_ENV="development"

# CORS
CORS_ORIGIN="http://localhost:3000"

# Email (Mock for now)
EMAIL_FROM="noreply@bootcamp.com"
EMAIL_ENABLED="false"
